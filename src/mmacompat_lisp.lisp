;; Barton Willis supplied Maxima function
;; m -- maxima matrix
;; return - nested list, if not a matrix, then fall through
;;(defun $mat_to_list (m)
;;  (if (eq '$matrix (car (car m))) 
;;      (cons '(mlist simp) (rest m))
;;      (return-from $mat_to_list m)))


;(compile '$nest)
;(compile '$fold)
